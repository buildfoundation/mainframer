apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion '25.0.2'

    defaultConfig {
        applicationId 'com.gojuno.mainframer.gradleandroidsample'
        minSdkVersion 25
        targetSdkVersion 25
    }
}
